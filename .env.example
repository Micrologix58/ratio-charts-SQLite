# Copy this file to .env (in this same directory) and fill in real values.
# .env is gitignored -- never commit real secrets.

# Home tab / Portfolio tab login gate (see /api/auth/* in index.js)
AUTH_USERNAME=
AUTH_PIN=
